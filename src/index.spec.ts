import { expect } from "chai";
import { Index } from "./index";

describe("index.js tests", () => {
  describe("#sample test()", () => {
    it("expect to write more tests", () => {
      // arranges

      // acts

      // asserts
      expect(Index).to.equal("Hello, TypeScripts!");
    });
  });
});
